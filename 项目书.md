# 校园场地预约系统 — 项目报告

---

## 一、项目概述与设计意图

### 1.1 项目目标

本系统为**浙江工业大学校园场地预约系统**，面向三类用户：

| 身份 | 说明 | 主要能力 |
|------|------|----------|
| **学生/老师** | 使用学号登录 | 查看公告、预约场地、查看我的预约、个人资料管理 |
| **场地管理员** | 使用工号登录 | 查看公告、审核预约申请、管理所辖场地、个人资料管理 |
| **系统管理员** | 使用用户名登录 | 系统总览、场地与账号管理、发布公告等 |

设计意图：通过**统一登录入口 + 角色区分**，让不同身份进入各自界面，既保证安全，又避免多套系统维护成本。

### 1.2 技术架构概览

- **前端**：Vue 3 + TypeScript + Element Plus + Pinia + Vue Router  
- **后端**：NestJS + TypeORM + MySQL  
- **认证**：JWT（JSON Web Token）+ 角色守卫（RolesGuard）  
- **密码**：bcrypt 加密存储，不明文保存  

---

## 二、流程图

### 2.1 整体业务流程图

```
用户打开系统 → 登录页选择身份（学生/老师、场地管理员、系统管理员）并输入账号密码
    → 后端校验账号密码并签发 JWT
    → 前端根据身份跳转：
        - 学生/老师：若未确认必读公告 → 强制进入公告页；确认后可进入场地列表/预约/我的预约等
        - 场地管理员：同上，确认公告后进入审核申请/场地管理
        - 系统管理员：直接进入总览/账号/场地/公告管理
```

### 2.2 登录流程

```
[用户] 选择身份 → 输入学号/工号/用户名、密码 → 点击登录
    ↓
[前端] 调用 POST /auth/login，请求体：{ role, account, password }
    ↓
[后端] 根据 role 查对应表（user / venue_admin / system_admin）
    ↓
[后端] 用 bcrypt.compare 校验密码
    ↓
[后端] 若正确：生成 JWT（内含 id、role、sub），返回 accessToken + 用户信息
    ↓
[前端] 将 token 存入 localStorage，用户信息存入 Pinia + localStorage
    ↓
[前端] 若为学生/老师或场地管理员：清除“已确认公告”标记（sessionStorage）
    ↓
[前端] 根据 role 跳转：user → /user/announcements 或 /user/change-password；venue_admin → /venue-admin/announcements；system_admin → /system-admin/overview
```

### 2.3 学生预约流程

```
进入场地列表 → 筛选/查询场地 → 点击场地卡片进入详情 → 点击“预约此场地”
    → 填写预约表单（日期、时间段、活动名、负责人等）→ 可选上传策划书（PDF/Word）
    → 前端调用“时段可用性检查”接口 → 若可用则提交申请
    → 后端创建预约记录，状态为“待审核”
    → 场地管理员在“审核申请”中通过/驳回
    → 通过/驳回后系统向申请人发送站内通知
```

---

## 三、登录界面：三种身份的设计与实现

### 3.1 设计思路

- **一个登录页**：不拆成三个网站，而是用**身份下拉框 + 同一套表单**完成三种身份登录。  
- **同一接口**：后端只提供一个 `POST /auth/login`，请求体里带 `role`（`user` | `venue_admin` | `system_admin`）和 `account`、`password`。  
- **标签随身份变化**：选“学生/老师”时显示“学号”，选“场地管理员”显示“工号”，选“系统管理员”显示“用户名”，避免用户填错栏位。

### 3.2 前端实现（Login.vue）

1. **表单数据**  
   - `form.role`：当前选中的身份，类型为 `'user' | 'venue_admin' | 'system_admin'`。  
   - `form.account`：账号（学号/工号/用户名）。  
   - `form.password`：密码。

2. **动态标签与占位符**  
   - 使用 Vue 的 `computed`：  
     - `accountLabel`：根据 `form.role` 返回“学号”“工号”或“用户名”。  
     - `accountPlaceholder`：返回“请输入学号”等提示。  
   - 这样无需写三个页面，仅通过一个下拉框切换即可改变表单项的语义。

3. **提交逻辑**  
   - 点击“登录”后调用 `login(form.role, form.account, form.password)`，即请求 `POST /auth/login`，请求体为 `{ role, account, password }`。  
   - 成功后：  
     - 把返回的 `accessToken`、用户信息写入 Pinia（`store.setLogin(payload)`），同时把 token 和用户信息持久化到 `localStorage`。  
     - 根据 `payload.role` 跳转：  
       - `user`：若需强制改密则跳 `/user/change-password`，否则跳 `/user/announcements`。  
       - `venue_admin`：跳 `/venue-admin/announcements`。  
       - `system_admin`：跳 `/system-admin/overview`。  
   - 失败则用 Element Plus 的 `ElMessage.error` 提示“学号或密码错误”等。

### 3.3 后端实现（auth.service.ts + auth.controller.ts）

1. **入口**  
   - 控制器接收 `LoginDto`：`role`、`account`、`password`（密码需至少 6 位，由 class-validator 校验）。

2. **按角色查表与验密**  
   - **学生/老师（role === 'user'）**：  
     - 用 `account` 在 `user` 表中查 `studentId`。  
     - 取该用户的 `passwordHash`（或兼容字段 `password_hash`），用 `bcrypt.compare(password, hash)` 校验。  
     - 失败则抛 `UnauthorizedException('学号或密码错误')`。  
   - **场地管理员（venue_admin）**：  
     - 用 `account` 在 `venue_admin` 表查 `workId`，同样用 bcrypt 校验密码，失败则“工号或密码错误”。  
   - **系统管理员（system_admin）**：  
     - 用 `account` 在 `system_admin` 表查 `username`，bcrypt 校验，失败则“用户名或密码错误”。

3. **签发 JWT**  
   - 使用 `@nestjs/jwt` 的 `JwtService.sign()`，payload 包含：  
     - `sub`：登录账号（学号/工号/用户名）  
     - `role`：当前身份  
     - `id`：对应用户/管理员的主键 id  
   - 密钥来自环境变量 `JWT_SECRET`（开发环境有默认值）。  
   - 返回给前端的 JSON 中包括：`accessToken`、`role`、`id`，以及姓名、电话、头像等展示用字段；学生/老师还有 `mustChangePassword`（是否强制修改密码）。

### 3.4 路由与权限（router/index.ts）

- **路由守卫 `beforeEach`**：  
  - 若目标路由需要登录（`meta.role`）而当前未登录，则重定向到登录页 `/`。  
  - 若已登录但访问的 `meta.role` 与当前用户 `store.role` 不一致，则重定向到该角色首页（如 `/user`、`/venue-admin`、`/system-admin`）。  
  - **学生/老师、场地管理员**：若 `store.announcementsConfirmed` 为 false，则除了公告页和改密页外，一律重定向到公告页，从而实现“不看完公告不能进入”的功能（见下文公告一节）。

小结：登录设计通过**单一接口 + 角色字段 + 三张用户表**实现三种身份统一入口，前端用计算属性切换标签，后端按 role 查不同表并签发同一格式的 JWT，再配合路由守卫和公告确认状态，完成权限与流程控制。

---

## 四、学生/老师界面功能说明

### 4.1 公告区域：必读公告、历史公告、页签与“不看完不能进入”

#### 4.1.1 页签如何实现

- 使用 **Element Plus 的 `el-tabs`**，`v-model="activeTab"` 绑定当前选中的页签，取值为 `'must-read'` 或 `'history'`。  
- 两个 `el-tab-pane`：  
  - **必读公告**（name="must-read"）：展示当前用户尚未“确认”的必读公告列表。  
  - **历史公告**（name="history"）：展示该用户已经确认过的必读公告（只读，无确认按钮）。  
- 切换页签时不需要重新请求接口，因为进入页面时已经同时请求了“必读未读”和“必读已读历史”两份数据，分别存在 `list` 和 `historyList` 中。

#### 4.1.2 必读公告如何获取与展示

- **接口**：`GET /announcements/must-read`（需带 JWT）。  
- **后端逻辑**（announcement.service.ts）：  
  - 查出所有“必读”且目标角色包含当前用户的公告：`isMustRead = 1` 且 `targetRole` 为当前角色或 `'all'`。  
  - 再查 `user_announcement_read` 表，得到该用户（userId + role）已读的公告 id 集合。  
  - 两者相减，得到**未读的必读公告**列表，按创建时间倒序返回。  
- 前端在 `onMounted` 里调用 `getMustRead()`，结果赋给 `list`，用 `el-card` 逐条展示标题、内容、时间，每条有“确认”按钮。

#### 4.1.3 历史公告如何实现

- **接口**：`GET /announcements/must-read/history`（需带 JWT）。  
- **后端逻辑**：  
  - 同样先查“必读且目标包含当前角色”的公告，再查该用户在 `user_announcement_read` 里的已读 id。  
  - 本次取**已读**的必读公告，按时间倒序，作为“历史公告”。  
- 前端请求结果赋给 `historyList`，在“历史公告”页签中只读展示，无按钮。

#### 4.1.4 公告“确认”（观看）如何实现

- 用户点击某条必读公告的“确认”按钮后，前端调用 `POST /announcements/mark-read`，请求体 `{ announcementIds: [该条 id] }`。  
- 后端在 `user_announcement_read` 表中插入一条记录：`userId`、`role`、`announcementId`，表示该用户已读。  
- 前端随后把该条从 `list` 中移除，并插入到 `historyList` 头部，这样“必读”列表减少、“历史”列表增加，无需刷新页面。

#### 4.1.5 “不看完公告不能进入”如何实现

- **状态来源**：  
  - 用户 store 中有 `announcementsConfirmed`（计算属性）：  
    - 若角色不是 user/venue_admin，直接为 true（系统管理员无需确认公告）。  
    - 否则看 `announcementsConfirmedRef`，其初始值来自 `sessionStorage.getItem('announcementsConfirmed') === '1'`。  
  - 登录成功时（setLogin）：若是 user 或 venue_admin，会清除 sessionStorage 的该键，并设 `announcementsConfirmedRef = false`，即“未确认公告”。  

- **何时变为“已确认”**：  
  - 在公告页，当“必读公告”列表为空（即没有未读必读）时，页面上会显示“进入主界面”按钮。  
  - 用户点击后调用 `store.setAnnouncementsConfirmed()`：  
    - 写 `sessionStorage.setItem('announcementsConfirmed', '1')`，并设 `announcementsConfirmedRef = true`。  
  - 然后跳转到 `/user/venue-list`（或场地管理员的 `/venue-admin/applications`）。  

- **路由守卫**：  
  - 在 `router.beforeEach` 中，若当前角色是 user 且 `!store.announcementsConfirmed`，且目标路径不是 `/user/announcements` 和 `/user/change-password`，则强制 `next('/user/announcements')`。  
  - 场地管理员同理，未确认时只能访问公告页。  

因此，“不看完公告不能进入” = **必读列表为空才出现“进入主界面”** + **点击后把 session 标记设为已确认** + **路由守卫根据该标记拦截**。注意：这里“看完”的判定是“当前没有未读的必读公告”，而不是“每条都点过确认”（点确认即从未读变为已读）。

#### 4.1.6 站内通知如何实现

- **数据来源**：`GET /notifications`（可带 `unread_only=true`）。后端根据 JWT 中的角色和 id，在 `notification` 表中查 `target_type`、`target_id` 匹配的记录，按创建时间倒序返回，每条含 id、title、content、createdAt、isRead。  
- **展示**：在公告页下方单独一块“站内通知”，用卡片列表展示；未读的可用左边框高亮（class `unread`）。  
- **删除**：学生/老师端每条通知有“×”删除按钮，调用 `DELETE /notifications/:id`，删除后从前端列表中移除。  
- **产生时机**：预约被通过/驳回、或场地被设为不可用导致预约被驳回时，后端会调用 `NotificationService.create` 给对应用户（或场地管理员）插入一条站内通知。

---

### 4.2 个人主页：上传头像、编辑资料、修改密码、退出登录

#### 4.2.1 上传头像

- **入口**：个人主页的“编辑资料”弹窗里，“头像”一项有预览和“上传头像”按钮。  
- **组件**：使用 Element Plus 的 `el-upload`，`accept="image/*"`，`:before-upload="onAvatarSelect"`，在 `before-upload` 中执行上传并 return false（阻止自动提交，由我们自行发请求）。  
- **请求**：`onAvatarSelect` 内调用 `uploadAvatar(file)`，即 `POST /upload/avatar`，请求体为 `FormData`，字段名为 `file`。  
- **后端**：`UploadController` 使用 `FileInterceptor('file')` 和 `diskStorage`，将文件存到 `uploads/avatars/`，文件名用时间戳+随机数+扩展名，限制 2MB；返回 `{ path: '/uploads/avatars/xxx.jpg' }`。  
- **前端**：收到 path 后写入 `editForm.avatar`，并更新本地预览（加时间戳防缓存）；保存资料时把该 path 一并提交到 `PATCH /auth/profile` 的 `avatar` 字段，并调用 `store.setProfile({ avatar })` 更新 Pinia 和 localStorage，这样顶部或侧栏头像会更新。

#### 4.2.2 编辑资料

- 点击“编辑资料”打开弹窗，表单预填当前 `store.user` 的 name、phone、头像 path。  
- 修改后点击“保存”，调用 `updateProfile({ name, phone, avatar })`（PATCH /auth/profile），成功后 `store.setProfile(...)` 并关闭弹窗。  
- 后端根据 JWT 的 role 和 id 更新对应用户表（user/venue_admin/system_admin）的 name、phone、avatar 字段。

#### 4.2.3 修改密码

- 点击“修改密码”跳转到 `/user/change-password`。  
- 表单为：原密码、新密码。提交时调用 `POST /auth/change-password`，body 为 `{ oldPassword, newPassword }`。  
- 后端用 bcrypt 校验原密码，通过后用 `bcrypt.hash(newPassword, 10)` 生成新哈希并写回数据库；若是学生且之前被标记为必须改密，会同时清除 `mustChangePassword`。

#### 4.2.4 退出登录

- 点击“退出登录”调用 `store.logout()`：清空 Pinia 的 token 和 user，删除 localStorage 的 `accessToken`、`user`，以及 sessionStorage 的 `announcementsConfirmed`。  
- 然后 `router.push('/')` 跳回登录页。

---

### 4.3 预约申请与文件上传

#### 4.3.1 如何进入预约申请

- 从“场地列表”点某场地卡片进入“场地详情”（`/user/venue/:id`），详情页有“预约此场地”按钮，点击后跳转到 `/user/booking-apply?venueId=xxx`，预约表单会预选该场地。  
- 也可从顶部菜单直接进“预约申请”，再在表单里选择场地。

#### 4.3.2 预约表单与时段校验

- 表单字段：场地、使用日期、开始时间、结束时间、活动名称、主办单位、预计人数、负责人、联系电话、活动说明、策划书（可选）。  
- **日期**：使用 `el-date-picker`，`disabled-date` 禁止选择今天之前的日期。  
- **时间**：使用 `el-time-select`，步长 30 分钟；若选择的是“今天”，开始时间的最小值会从“当前时间之后第一个半点”开始算（如当前 14:20，则最小开始时间为 14:30），避免选到已过去时段。  
- **时段可用性**：当场地、日期、开始、结束都选好后，前端会调用 `GET /booking-applications/check-availability?venue_id&use_date&start_time&end_time`。  
  - 后端会：1）判断是否已过去（past）；2）查该场地同一天、状态为 pending/approved 且时间重叠的预约（算法：start_time < 请求的 end_time 且 end_time > 请求的 start_time）；3）查该场地在该时间段是否有“不可用时段”（venue_unavailable_slot）。  
  - 返回 `{ available, conflict, hasUnavailableSlot, past }`。  
- 前端根据返回显示“该时段可预约”“已被预约或不可用”“已过时”等标签；提交时再次校验，若不可用或已过则提示不提交。

#### 4.3.3 策划书（文件）上传

- 表单项使用 `el-upload`，`accept=".pdf,.doc,.docx"`，`:on-change="onFileChange"`，`:auto-upload="false"`（不自动上传，在 onChange 里自己调接口）。  
- `onFileChange` 中取 `file.raw`，调用 `uploadProposal(file)`，即 `POST /upload/proposal`，FormData 字段名为 `file`。  
- 后端限制 10MB，存到 `uploads/proposals/`，返回 `{ path: '/uploads/proposals/xxx.pdf' }`。  
- 前端把 path 赋给 `form.proposalDocumentPath`，提交预约时一并传给后端，存入 `booking_application.proposal_document_path`。  
- 场地管理员在“审核申请”的详情里可以看到“策划书”链接，点击即可打开该文件。

#### 4.3.4 提交申请

- 必填项校验通过且时段可用时，调用 `createBooking(form)`，即 `POST /booking-applications`，body 包含所有表单字段。  
- 后端再次校验时间未过、无冲突、无不可用时段，然后在 `booking_application` 表插入一条记录，status 为 `pending`，并关联当前用户 id 和场地 id。  
- 成功后前端提示并跳转到“我的预约”页。

---

### 4.4 场地列表、筛选、查询与“我的预约”

#### 4.4.1 场地列表如何展示

- 页面进入时调用 `getVenueList(filters)`，即 `GET /venues`，可带查询参数。  
- 后端使用 TypeORM 的 `createQueryBuilder`，按条件拼接：`venue_type`、`min_capacity`、`max_capacity`、`location`（LIKE）、`has_projector`、`has_sound`、`is_available` 等，返回场地数组。  
- 前端用 `el-row`/`el-col` 做卡片网格，每个场地一张卡片：首图（photos[0]）、名称、类型、容量、位置、设备、可用标签；点击卡片进入 `getVenue(id)` 的详情页。

#### 4.4.2 筛选与查询如何实现

- 页面上方是筛选表单（`el-form :inline="true"`），绑定 `filters` 对象：  
  - 类型：下拉，可选报告厅/会议室/活动中心（value 为 report_hall、meeting_room、activity_center）。  
  - 容量：最小、最大（数字输入）。  
  - 位置：文本输入，后端用 LIKE %location%。  
  - 设备：勾选“投影”“音响”，对应 has_projector、has_sound 为 true。  
  - 可用：下拉“全部/可用/不可用”，对应 is_available。  
- 点击“查询”时，把 `filters` 里已填项整理成 `VenueFilters` 参数（只传有值的），调用 `getVenueList(params)` 重新拉列表。  
- “重置”即清空 filters 再调一次 `load()`。

#### 4.4.3 我的预约

- 菜单“我的预约”对应 `/user/my-bookings`，进入时请求 `GET /booking-applications/my`（带 JWT）。  
- 后端根据 JWT 的 user id 查 `booking_application` 表，带出 venue 关联，按日期、时间倒序返回。  
- 前端用表格展示：ID、场地、使用日期、时间段、活动名称、状态（待审核/已通过/已驳回/已使用/已取消）、操作（详情、取消）。  
- “取消”仅对待审核或已通过、且使用时间尚未开始的记录开放；点击后确认框，再调 `POST /booking-applications/:id/cancel`，后端将 status 改为 `cancelled`。

---

## 五、场地管理员界面功能说明（详细）

下面按功能拆开，**前端怎么做**和**后端怎么做**分开写，方便零基础读者对照理解。

---

### 5.1 公告与“不看完不能进入”

- **前端**：与学生端相同。登录后若未确认公告，路由守卫会强制停留在 `/venue-admin/announcements`。页面有“必读公告”“历史公告”两个页签（el-tabs）和“站内通知”区域。必读列表为空时显示“进入主界面”按钮，点击后调用 `store.setAnnouncementsConfirmed()` 并跳转到 `/venue-admin/applications`。  
- **后端**：使用同一套公告接口。`GET /announcements/must-read`、`GET /announcements/must-read/history`、`POST /announcements/mark-read` 会根据 JWT 中的 role=venue_admin 筛选 targetRole 为 venue_admin 或 all 的必读公告，并在 user_announcement_read 表中按 userId + role 记录已读。

---

### 5.2 个人主页、修改密码、退出登录

- 与学生/老师完全一致：同一套 `getProfile`、`updateProfile`、`uploadAvatar` 接口，后端根据 JWT 的 id 和 role=venue_admin 操作 venue_admin 表。修改密码、退出登录流程相同，仅路由为 `/venue-admin/change-password`，退出后 `router.push('/')`。

---

### 5.3 审核申请：待审核、审批记录、预约总览（前后端分开说明）

审核申请页（`/venue-admin/applications`，组件 ReviewApplications.vue）用 **el-tabs** 做三个页签：待审核、审批记录、预约总览。三个页签对应三份数据、三个接口，下面分别写清前端如何请求与展示、后端如何查数据。

#### 5.3.1 待审核 — 前端实现

- **数据从哪来**：页面挂载时（onMounted）会同时调用 `loadPending()`、`loadHistory()`、`loadOverview()`。待审核数据来自 `loadPending()` 内部调用的 `getPendingList()`，即请求 **GET /booking-applications/pending**，请求头带 JWT。
- **表格展示**：返回的数组赋给 `pendingList`。表格（el-table）绑定 `pendingList`，列包括：ID、场地（row.venue?.name）、使用日期、时间段（row.startTime - row.endTime）、活动名称、申请人（row.user?.name 或 row.user?.studentId）、操作。
- **操作按钮**：每一行有“详情”“通过”“驳回”。  
  - **详情**：把当前行赋给 `current`，打开详情弹窗（el-dialog），展示场地、使用时间、活动名称、主办单位、预计人数、负责人与电话、说明、策划书（若有 proposalDocumentPath 则用 `<a :href="..." target="_blank">查看</a>`）。  
  - **通过**：先弹出 ElMessageBox.confirm 确认，用户确定后调用 `approveBooking(row.id)`，即 **POST /booking-applications/:id/approve**（无 body）。成功后提示“已通过”，并重新执行 loadPending、loadHistory、loadOverview 刷新三个列表。  
  - **驳回**：点击后打开“驳回理由”弹窗，用户输入 `rejectReason`，点击“确认驳回”调用 `rejectBooking(current.id, rejectReason)`，即 **POST /booking-applications/:id/reject**，body 为 `{ rejectReason }`。成功后关闭弹窗并刷新三个列表。

#### 5.3.2 待审核 — 后端实现

- **接口**：GET /booking-applications/pending，需要 JwtAuthGuard + RolesGuard，且角色为 venue_admin。
- **逻辑**（booking-application.service.ts 的 findPendingForVenueAdmin）：  
  1）根据当前 JWT 中的管理员 id，调用 venueService.findManagedByVenueAdmin(adminId)，查出该管理员负责的场地列表（多对多表 venue_admin_venue 关联）。  
  2）用这些场地的 id 列表，在 booking_application 表中查 venue_id 属于该列表且 status = 'pending' 的记录。  
  3）关联查询 venue、user（relations: ['venue', 'user']），按 createdAt 正序（先申请的在前）。  
  4）返回该数组。  
- **通过**：POST /booking-applications/:id/approve。后端 findOne(id, { venueAdminId }) 校验该预约是否属于该管理员负责的场地；校验通过后把 status 改为 'approved'，写入 approved_by、approved_at，并调用 NotificationService.create 给该预约的 userId 发一条站内通知（title: 预约已通过，content 含活动名）。  
- **驳回**：POST /booking-applications/:id/reject，body 含 rejectReason。同样校验权限后，把 status 改为 'rejected'，写入 reject_reason、approved_by、approved_at，并发站内通知“预约已驳回”及原因。

#### 5.3.3 审批记录 — 前端实现

- **数据从哪来**：loadHistory() 里调用 `getHistoryList()`，即 **GET /booking-applications/history**（带 JWT）。
- **表格**：数据赋给 `historyList`。表格列比待审核多一列“状态”（用 statusText/statusType 显示待审核/已通过/已驳回等）和“审批时间”（approvedAt）。没有通过/驳回按钮，仅作历史查看。

#### 5.3.4 审批记录 — 后端实现

- **接口**：GET /booking-applications/history。  
- **逻辑**（findHistoryForVenueAdmin）：同样先查该管理员负责的场地 id 列表，再查 booking_application 中 venue_id 在该列表中、**不限 status** 的所有记录，带 venue、user、approvedByAdmin，按 createdAt 倒序返回，方便看最近审批的在前。

#### 5.3.5 预约总览 — 前端实现

- **数据从哪来**：loadOverview() 里调用 `getOverview()`，即 **GET /booking-applications/overview**（带 JWT）。
- **表格**：数据赋给 `overviewList`。列包括 ID、场地、使用日期、时间段、活动名称、申请人、状态。用于快速看“即将到来的预约”有哪些，不区分待审核/已通过，都显示。

#### 5.3.6 预约总览 — 后端实现

- **接口**：GET /booking-applications/overview。  
- **逻辑**（getOverviewForVenueAdmin）：先得到该管理员负责的场地 id 列表，再查 booking_application 中 venue_id 在该列表中且 status 为 'pending' 或 'approved' 的记录，带 venue、user，按 useDate、startTime 正序，即按时间从早到晚排，方便管理员看日程。

---

### 5.4 场地管理：编辑、上传照片、照片在学生界面的展示（前后端分开）

场地管理页（`/venue-admin/venues`，组件 VenueManage.vue）只展示**当前场地管理员负责的场地**，支持编辑、上传照片、添加不可用时段。下面分“列表与编辑”“上传照片”“学生端如何看到照片”三块写。

#### 5.4.1 场地列表与编辑 — 前端实现

- **列表数据**：onMounted 时调用 `getManagedVenues()`，即 **GET /venues/managed/list**（带 JWT）。返回的数组赋给 `list`，表格显示 ID、名称、类型、容量、位置、照片数量（row.photos?.length 或“无”）、状态（可用/不可用）、操作（编辑、不可用时段）。
- **编辑**：点击某行的“编辑”时，把该行数据拷贝到 `editForm` 对象中（id、name、location、address、capacity、openTime/closeTime 转为 openTimeVal/closeTimeVal、hasProjector、hasSound、isAvailable、photos 数组），然后打开编辑弹窗（el-dialog）。  
  - 表单字段：名称、位置、地址、容量、开放时间（两个 el-time-picker）、设备（投影/音响两个 checkbox）、可用（el-switch，1/0）、场地照片（见下）。  
  - 用户改完后点“保存”，调用 `updateVenue(editForm.id, { name, location, address, capacity, openTime, closeTime, hasProjector, hasSound, isAvailable, photos })`，即 **PUT /venues/:id**，body 为上述字段。成功后关闭弹窗并重新 load 列表。

#### 5.4.2 场地列表与编辑 — 后端实现

- **列表接口**：GET /venues/managed/list。根据 JWT 中的 venue_admin id，在 TypeORM 中查 VenueAdmin 并 relations: ['venues']，返回 admin.venues，即该管理员关联的场地数组。
- **更新接口**：PUT /venues/:id。VenueService.update 会先根据 id 查场地是否存在；若不是系统管理员，会用 SQL 查 venue_admin_venue 表，判断当前 venue_admin_id 是否管理该 venue_id，无权限则抛 ForbiddenException。有权限则把 body 中的字段更新到 venue 表（name、location、address、capacity、openTime、closeTime、hasProjector、hasSound、isAvailable、photos）。若 isAvailable 从 1 改为 0，会调用 notifyVenueUnavailable，把该场地所有 pending/approved 预约改为 rejected 并给申请人发站内通知。

#### 5.4.3 上传照片 — 前端实现

- **入口**：在“编辑场地”弹窗里，“场地照片”表单项中有一个“上传图片”按钮，使用 **el-upload**，accept="image/*"，:before-upload="beforePhotoUpload"。
- **流程**：用户选择文件后，beforePhotoUpload 被调用。前端检查文件大小（例如不超过 15MB），若超则提示并 return false；否则调用 `uploadVenuePhoto(file)`，即 **POST /upload/venue-photo**，请求体为 FormData，字段名为 file。不自动上传（由 before-upload 里发请求），最后 return false 阻止 el-upload 默认上传。
- **上传成功后**：接口返回 `{ path: '/uploads/venues/xxx.jpg' }`，前端把 path  push 到当前 editForm.photos 数组中，并提示“已添加图片”。此时弹窗里会多出一张缩略图；用户可点击某张照片的“删除”从 editForm.photos 中 splice 掉。点“保存”时，整个 editForm.photos 数组会随 PUT /venues/:id 一起提交，后端把 photos 存到 venue 表的 photos 字段（JSON 数组）。

#### 5.4.4 上传照片 — 后端实现

- **接口**：POST /upload/venue-photo，需要 JWT 且角色为 venue_admin 或 system_admin。使用 FileInterceptor('file')，storage 为 diskStorage，目录为 `./uploads/venues`（或环境变量 UPLOAD_DIR 下的 venues），文件名：时间戳 + 随机数 + 扩展名，限制 15MB。保存成功后返回 `{ path: '/uploads/venues/文件名' }`。该 path 会被前端放进 venue 的 photos 数组，在 PUT /venues/:id 时一起写入数据库。

#### 5.4.5 照片在学生界面的展示 — 前端实现

- **场地列表页（学生/老师）**：getVenueList 返回的每条场地对象中带有 `photos` 数组（后端 venue 表 JSON 字段）。列表卡片上，若 `v.photos?.length` 有值，则用第一张图做封面：`<img :src="photoSrc(v.photos[0])" />`。photoSrc 函数：若 path 不以 `/` 开头则前面拼一个 `/`，这样图片请求路径为站内绝对路径，指向后端静态资源或前端 public。  
- **场地详情页（学生/老师）**：getVenue(id) 返回的 venue 同样带 photos 数组。详情页用 `v-for="(url, i) in venue.photos"` 遍历，每张图用 `<a :href="photoSrc(url)" target="_blank"><img :src="photoSrc(url)" /></a>` 展示，可点击放大或新标签打开。  
- 因此：**场地管理员在“场地管理”里编辑场地并上传的照片，保存后写入 venue.photos；学生端拉取的场地列表和详情都包含 photos，列表用首图做封面，详情页展示全部照片。**

#### 5.4.6 不可用时段

- **前端**：点击“不可用时段”打开弹窗，表单为 venueId（当前行 id）、startTime、endTime（el-date-picker type="datetime"）、reason。提交时调用 `addUnavailableSlot(venueId, { startTime, endTime, reason })`，即 **POST /venues/:id/unavailable-slots**。  
- **后端**：在 venue_unavailable_slot 表插入一条记录；并会对与该时间段有交集的、该场地的 pending/approved 预约做驳回处理，并给申请人发站内通知。

---

## 六、系统管理员界面功能说明（详细）

系统管理员登录后进入 `/system-admin/overview`，菜单包含：总览（预约总览+可视化）、场地管理、账号管理、公告等。下面按功能分块，**前端**和**后端**分开写，便于零基础读者逐条对照。

---

### 6.1 预约总览（全校）：详细信息、分“未驳回/已驳回”、删除

对应页面：**Overview.vue**（路由 /system-admin/overview）。

#### 6.1.1 前端实现

- **两个页签**：用 el-tabs，v-model="activeTab"，两个 el-tab-pane：name="detail"（详细信息）、name="visual"（可视化分析）。
- **详细信息页签**：  
  - **数据来源**：onMounted 时调用 `loadData()`，内部同时请求 `getAllBookings()` 和 `getVenueList()`。getAllBookings 即 **GET /booking-applications/all**（带 JWT，角色 system_admin）。返回全校所有预约记录，赋给 `list`。  
  - **筛选**：页面上方有一个 el-radio-group，v-model="statusFilter"，三个选项：全部（all）、未驳回（not_rejected）、已驳回（rejected）。  
  - **表格数据**：表格不直接绑定 list，而是绑定计算属性 **filteredList**。filteredList 根据 statusFilter 过滤：all 时返回全部 list；rejected 时返回 list 中 status === 'rejected' 的项；not_rejected 时返回 list 中 status !== 'rejected' 的项。这样用户点选“未驳回”或“已驳回”时，表格只显示对应数据。  
  - **表格列**：ID、场地、使用日期、时间段、活动名称、申请人、状态（待审核/已通过/已驳回等）、操作（删除）。  
  - **删除**：点击某行的“删除”，先 ElMessageBox.confirm 确认，再调用 `deleteBooking(row.id)`，即 **DELETE /booking-applications/:id**。成功后重新 loadData()，列表和可视化数据会一起更新。

#### 6.1.2 后端实现

- **接口**：GET /booking-applications/all，需 JwtAuthGuard + RolesGuard，角色 system_admin。  
- **逻辑**（BookingApplicationService.findAllForSystemAdmin）：直接查 booking_application 表，relations: ['venue', 'user']，order 按 useDate、startTime 倒序，返回全部记录，不做场地范围限制。  
- **删除**：DELETE /booking-applications/:id，仅 system_admin 可调。Service 中 deleteBySystemAdmin(id) 根据 id 查记录，若存在则 repo.remove(booking)，物理删除该条预约。

---

### 6.2 可视化分析：选择场地、各时段预约次数；各场地近期预约次数

仍在 **Overview.vue** 的“可视化分析”页签内，全部在前端用已有数据计算，不再请求新接口。

#### 6.2.1 按场地查看各时段预约情况 — 前端实现

- **选择场地**：页面上有一个 el-select，v-model="selectedVenueId"，选项来自 `venues`（即 loadData 时 getVenueList() 返回的全校场地列表），placeholder 为“选择场地”，可清空。  
- **数据来源**：使用“未驳回的成功预约”参与统计。计算属性 **successList** = list 中 status 为 'approved' 或 'used' 的项。  
- **时段划分**：前端写死一个 **TIME_SLOTS** 数组，如 08:00-09:00、09:00-10:00 … 21:00-22:00，共 15 个时段。  
- **每个时段的预约次数**：计算属性 **venueSlotData** 依赖 selectedVenueId 和 successList。当 selectedVenueId 有值时，对 TIME_SLOTS 每个时段，统计 successList 里“场地 id = selectedVenueId 且 预约时间段与该时段有重叠”的条数。重叠判断：把时段和预约的 start/end 都转成“从 0 点起的分钟数”，用区间相交条件（开始 < 时段结束 且 结束 > 时段开始）计数。得到每个时段的 count，再按最大 count 算 ratePercent 用于柱状颜色深浅。  
- **展示**：用一排 div（timeline-axis）展示每个时段，显示时段缩写和 count；有预约的时段用不同背景色标识，hover 可看“某时段：N 次”。若未选场地，提示“请先选择场地”。

#### 6.2.2 各场地近期预约次数 — 前端实现

- **数据**：同样用 successList（未驳回的成功预约）。取“近 30 天”：用当前日期减 30 天得到 cutoffStr，过滤 useDate >= cutoffStr 的预约。  
- **按场地汇总**：用 Map 按 venueId 累加条数，同时记下 venue 名称（从 row.venue?.name）。得到数组后按 count 降序排，再算每条占最大值的百分比 percent，用于条形图宽度。  
- **展示**：每个场地一行：场地名称、一条横向 bar（宽度为 percent%）、右侧显示“N 次”。页面说明“统计未驳回的成功预约，近 30 天；与详细信息联动，删除后此处同步更新”。

---

### 6.3 场地管理（系统管理员）：手动添加、下载模板、Excel 导入、恢复测试、一键清空、完全清空、筛选、编辑

对应页面：**VenueManage.vue**（路由 /system-admin/venues），与场地管理员的场地管理不同，这里管理的是**全校**场地。

#### 6.3.1 前端实现（按按钮说明）

- **手动添加**：点击“手动添加”打开弹窗，表单：名称（必填）、类型（下拉 report_hall/meeting_room/activity_center）、容量（数字）、地址（选填）、可用（开关）。提交时调用 `createVenue({ name, venueType, capacity, address, isAvailable })`，即 **POST /venues**。成功后关闭弹窗并 load() 刷新列表。  
- **下载 Excel 模板**：点击“下载 Excel 模板”调用 `downloadVenueTemplate()`。该函数请求 **GET /venues/import-template**（responseType: 'arraybuffer'），拿到二进制后转成 Blob，创建 a 标签并设置 download 文件名（如“场地导入模板.xlsx”），触发点击下载，不打开新页。  
- **Excel 导入**：使用 el-upload，accept=".xlsx,.xls"，:before-upload="onImportFile"。用户选文件后，onImportFile 里用 FormData 装 file，请求 **POST /venues/import**（注意用 fetch 并带 Authorization 头，因为可能走 FormData）。成功后返回 `{ created, errors }`，前端提示“导入完成：新增 N 个场地”，若有 errors 可提示前几条；然后 load() 刷新。return false 阻止 el-upload 默认上传。  
- **恢复测试场地**：点击“恢复测试场地”调用 `ensureTestVenues()`，即 **POST /venues/ensure-test**。返回 `{ created }`，若 created > 0 提示“已恢复 N 个测试场地”，否则“测试场地已存在，无需恢复”；然后 load()。  
- **一键清空（保留测试）**：点击后弹出确认框，说明“将删除除每类保留一个测试场地外的所有场地”。确认后调用 `clearVenues({ keepTestOnly: true })`，即 **POST /venues/clear**，body 为 { keepTestOnly: true }。成功后关闭确认框并 load()。  
- **完全清空**：确认框说明“将删除所有场地，不可恢复”。确认后调用 `clearVenues({ clearAll: true })`。  
- **删除选中**：表格带 type="selection" 的列，@selection-change 把选中的行 id 存到 selectedIds。点击“删除选中”时若 selectedIds 为空则提示“请先勾选”；否则弹出确认框，确认后调用 `clearVenues({ venueIds: selectedIds })`。  
- **筛选**：表头上方有筛选栏，表单项：类型（下拉）、可用状态（下拉）、容量（最小、最大）、名称（关键词）。点击“查询”调用 `applyFilter()`，内部用 buildParams() 把 filters 转成请求参数（只传有值的），再 `getVenueList(params)` 即 **GET /venues** 带 query，得到 list。注意：系统管理员端的“筛选后列表”可能还在前端做了一次名称关键词过滤和按名称排序（filteredList 计算属性），具体以代码为准。  
- **编辑**：表格每行有“编辑”按钮。点击后把该行 id、name、address、isAvailable 赋给 editForm，打开编辑弹窗。弹窗里只有名称、地址、可用开关（系统管理员简化版）。保存时 **PUT /venues/:id**，body 为 { name, address, isAvailable }，成功后 load()。

#### 6.3.2 后端实现（按接口说明）

- **POST /venues**：仅 system_admin。CreateVenueDto 含 name、venueType、capacity、location、address、openTime、closeTime、hasProjector、hasSound、isAvailable、photos 等；Service.create 往 venue 表 insert 一条。  
- **GET /venues/import-template**：返回 Excel 二进制。Service.getVenueTemplateBuffer() 用 xlsx 库生成一个工作簿，表头为名称、类型、容量、位置、地址、开放时间、关闭时间、投影、音响、可用，并带一行示例和类型说明，返回 buffer；Controller 设 Content-Disposition 为 attachment。  
- **POST /venues/import**：接收 file（Excel），用 memoryStorage 读入 buffer。Service.importVenuesFromBuffer 解析 Excel，按行读“名称、类型、容量…”等列，类型校验（report_hall/meeting_room/activity_center），逐行 insert venue，返回 { created, errors }。  
- **POST /venues/ensure-test**：Service.ensureTestVenues() 检查每种 venueType 是否至少有一条记录，没有则插入默认测试场地（如第一报告厅、第二会议室、学生活动中心）。返回 { created }。  
- **POST /venues/clear**：body 可为 keepTestOnly、clearAll、venueIds。keepTestOnly 时按类型保留每类一个，其余删；clearAll 时删全部；venueIds 时只删指定 id。删除前会先删关联的预约、不可用时段、venue_admin_venue 再删 venue。  
- **GET /venues**：无角色限制（或仅登录），按 query 参数 venue_type、min_capacity、max_capacity、location、has_projector、has_sound、is_available 等过滤，返回场地列表。  
- **PUT /venues/:id**：venue_admin 或 system_admin。Service.update 会校验权限（非系统管理员时查 venue_admin_venue）；然后更新传入的字段（系统管理员可能只传 name、address、isAvailable）。

---

### 6.4 账号管理：学生/老师、场地管理员列表，添加、导入、清空、发送通知、管辖范围

对应页面：**AccountManage.vue**（路由 /system-admin/accounts）。

#### 6.4.1 前端实现（按功能说明）

- **两个页签**：el-tabs，一页“学生/老师”（users），一页“场地管理员”（venueAdmins）。  
- **学生/老师列表**：onMounted 时 load() 调用 `getUsers()`，即 **GET /system-admin/users**，数据赋给 `users`。表格列：多选、ID、学号、姓名、电话、操作（发送通知）。  
- **场地管理员列表**：load() 里还调用 `getVenueAdmins()`（**GET /system-admin/venue-admins**）和 `getVenueList()`（用于管辖范围弹窗里的场地列表）。表格列：多选、ID、工号、姓名、管辖场地（(row.venues||[]).map(v=>v.name).join('、')）、操作（发送通知、管辖范围）。  
- **手动添加**：点击“手动添加”打开弹窗，表单：账号类型（学生/老师 或 场地管理员）、学号或工号、姓名、初始密码（选填，默认 123456）。提交时若类型为学生则 **POST /system-admin/accounts/users**，body { studentId, name, password }；若为场地管理员则 **POST /system-admin/accounts/venue-admins**，body { workId, name, password }。成功后关闭弹窗并 load()。  
- **下载 Excel 模板**：请求 **GET /system-admin/accounts/import-template**（responseType: 'arraybuffer'），下载为“账号导入模板.xlsx”。  
- **批量导入**：el-upload 选 .xlsx/.xls 后，before-upload 里用 FormData 发 **POST /system-admin/accounts/import**，返回 { usersCreated, venueAdminsCreated, errors }，提示“导入完成：学生 N 人，场地管理员 N 人”，若有 errors 可提示前几条，再 load()。  
- **恢复测试账号**：**POST /system-admin/accounts/ensure-test**。返回 { userCreated, venueAdminCreated }，若有任一为 true 则提示已创建测试学生/测试场地管理员，并 load()。  
- **一键清空（保留测试）**：弹窗里先选清空范围（仅学生/老师、仅场地管理员、全部），确认后 **POST /system-admin/accounts/clear**，body { keepTestOnly: true, clearScope }。  
- **完全清空**：同上，body 为 { clearAll: true, clearScope }。  
- **删除选中**：勾选若干行后点“删除选中”，body 为 { userIds: selectedUserIds, venueAdminIds: selectedVenueAdminIds }。  
- **发送通知**：某行点“发送通知”，打开弹窗，填标题和内容，提交时若目标为学生则 **POST /system-admin/notify-user**，body { targetId, title, content }；若为场地管理员则 **POST /system-admin/notify-venue-admin**。后端会在 notification 表插入一条 target_type、target_id 对应的记录，学生/场地管理员登录后可在“站内通知”里看到。  
- **管辖范围**：仅场地管理员行有“管辖范围”。点击后打开弹窗，显示当前管理员姓名、工号，下列表为全部场地（allVenues）的 checkbox，v-model 为 scopeVenueIds（当前管理员已关联的 venue id 数组）。用户勾选/取消后点“保存”，调用 **POST /system-admin/venue-admins/:id/scope**，body { venueIds }。后端会更新该管理员与场地的多对多关系，即该管理员之后只能审核这些场地下的预约。

#### 6.4.2 后端实现（简要）

- **GET /system-admin/users**：查 user 表全部，按 id 排序返回。  
- **GET /system-admin/venue-admins**：查 venue_admin 表并 relations: ['venues']，返回带管辖场地列表。  
- **POST /system-admin/accounts/users**：校验学号不重复，密码不足 6 位则用默认 123456 的 bcrypt 哈希，insert user，must_change_password 设为 1。  
- **POST /system-admin/accounts/venue-admins**：工号不重复，同上写 venue_admin 表。  
- **POST /system-admin/accounts/ensure-test**：若学号 2024001 不存在则插入测试学生，若工号 va001 不存在则插入测试场地管理员，默认密码 123456。  
- **POST /system-admin/accounts/clear**：按 clearScope 和 keepTestOnly/clearAll/userIds/venueAdminIds 删除对应用户或场地管理员；删除前会处理预约表、venue_admin_venue 表等关联。  
- **POST /system-admin/accounts/import**：解析 Excel，“学生”或“用户”工作表按学号、姓名、密码列批量 insert user；“场地管理员”工作表按工号、姓名、密码列批量 insert venue_admin。返回 usersCreated、venueAdminsCreated、errors。  
- **GET /system-admin/accounts/import-template**：生成含“学生”“场地管理员”两个 sheet 的 Excel，表头学号/姓名/初始密码、工号/姓名/初始密码，带示例行，返回 buffer。  
- **notify-user / notify-venue-admin**：NotificationService.create 插入 notification，targetType 为 user 或 venue_admin，targetId 为对应用户或管理员 id。  
- **POST /system-admin/venue-admins/:id/scope**：根据 id 查 VenueAdmin 并 load venues，把 admin.venues 设为 body.venueIds 对应的 Venue 数组，save。即更新 venue_admin_venue 中间表。

---

### 6.5 发布公告与观看历史公告

对应页面：**Announcements.vue**（路由 /system-admin/announcements）。

#### 6.5.1 发布公告 — 前端实现

- **表单**：标题（el-input）、内容（textarea）、登录必读（el-switch，v-model="form.isMustRead"）、若必读则显示“目标角色”（el-radio-group：学生/老师、场地管理员、全部）。提交时调用 `createAnnouncement(form)`，即 **POST /announcements**，body 为 { title, content, isMustRead, targetRole }。成功后清空表单并执行 load() 刷新下方“历史公告”表格。

#### 6.5.2 发布公告 — 后端实现

- **接口**：POST /announcements，需 system_admin。CreateAnnouncementDto 含 title、content、isMustRead、targetRole（可选）。Service.create 在 announcement 表 insert 一条，is_must_read、target_role 写入；created_by 为当前系统管理员 id。必读公告的 target_role 决定谁登录后会在“必读公告”里看到（user/venue_admin/all）。

#### 6.5.3 历史公告（观看与删除）— 前端实现

- **列表**：同一页下方有“历史公告”表格。load() 调用 `getAnnouncementList()`，即 **GET /announcements**（该接口若未加守卫则可能为公开或仅登录）。返回所有公告列表，赋给 `list`。表格列：标题、发布时间（createdAt）、必读（是/否）、目标角色（学生/老师、场地管理员、全部或 -）、操作（删除）。  
- **删除**：点击某行“删除”先弹出确认框，确认后调用 `deleteAnnouncement(row.id)`，即 **DELETE /announcements/:id**。成功后 load() 刷新表格。  
- 系统管理员在此看到的“历史公告”即**全部已发布的公告**，用于回顾或删除，不需要像学生/场地管理员那样区分“必读未读”和“必读已读历史”；区分必读未读/已读是学生端和场地管理员端通过 must-read、must-read/history 接口实现的。

#### 6.5.4 历史公告 — 后端实现

- **GET /announcements**：AnnouncementController 中 list() 无守卫或仅登录，直接 service.findAll()，按 createdAt 倒序返回所有公告。  
- **DELETE /announcements/:id**：需 system_admin。Service.delete 先删 user_announcement_read 中该 announcement_id 的记录，再删 announcement 表该条。

---

## 七、数据结构和算法

### 7.1 核心表结构（简要）

- **user**：id, student_id, name, phone, avatar, password_hash, must_change_password 等。  
- **venue_admin**：id, work_id, name, phone, avatar, password_hash；与 venue 多对多（venue_admin_venue）。  
- **system_admin**：id, username, phone, avatar, password_hash。  
- **venue**：id, name, venue_type, capacity, location, address, open_time, close_time, has_projector, has_sound, is_available, photos(JSON) 等。  
- **booking_application**：id, user_id, venue_id, use_date, start_time, end_time, activity_name, organizer, estimated_people, contact_name, contact_phone, description, proposal_document_path, status, reject_reason, approved_by, approved_at 等。  
- **announcement**：id, title, content, is_must_read, target_role, created_by, created_at。  
- **user_announcement_read**：user_id, role, announcement_id, read_at（联合主键）。  
- **notification**：id, target_type, target_id, title, content, is_read, created_at。  
- **venue_unavailable_slot**：id, venue_id, start_time, end_time, reason。

### 7.2 时段冲突判断算法

- **问题**：判断某场地在某日期 [startTime, endTime) 是否已被占用。  
- **做法**：查询该场地、该日期下 status 为 pending 或 approved 的预约，存在时间重叠即冲突。  
- **重叠条件**（两个区间 [a1,a2)、[b1,b2) 相交）：`a1 < b2 && a2 > b1`。  
- SQL 条件等价为：`start_time < :endTime AND end_time > :startTime`。  
- 若同时考虑“不可用时段”表，则再查 venue_unavailable_slot 中与该时间段有交集的记录，有则不可用。

### 7.3 必读公告“未读”的判定

- 算法：  
  1）查出所有 is_must_read=1 且 target_role 包含当前角色的公告 A；  
  2）查出 user_announcement_read 中该 userId+role 的 announcement_id 集合 R；  
  3）未读 = A 中 id 不在 R 中的公告。  
- 历史已读 = A 中 id 在 R 中的公告。

---

## 八、设计模式与实现要点

- **前后端分离**：前端 Vue SPA，后端 REST API，通过 JWT 在请求头 Authorization 中传递，axios 拦截器统一加 token、统一处理 401 登出。  
- **角色权限**：使用 NestJS 的守卫链——JwtAuthGuard 验证 token 并注入 user，RolesGuard 根据 @Roles() 装饰器检查 user.role，不通过则 403。  
- **单一登录入口 + 多表用户**：策略模式思想——根据 role 选择不同的 Repository 和校验逻辑，对外统一返回 token 与用户信息结构。  
- **公告“必须确认”**：用 sessionStorage 标记 + 路由守卫做前端强制流；后端只负责“已读”记录，不强制接口调用顺序。

---

## 九、如何使用本软件（简要）

1. **启动**：后端 `npm run start:dev`（或生产方式），前端 `npm run dev`，浏览器访问前端地址。  
2. **登录**：选择身份（学生/老师、场地管理员、系统管理员），输入对应账号密码，点击登录。  
3. **学生/老师**：首次需在公告页确认完必读公告后点“进入主界面”，然后可浏览场地、预约、查看我的预约、个人资料与修改密码。  
4. **场地管理员**：同样先确认公告，再在“审核申请”中处理待审核预约，在“场地管理”中维护所辖场地与不可用时段。  
5. **系统管理员**：登录后直接使用总览（预约总览与可视化）、账号管理、场地管理、发布与历史公告等；各功能的具体操作与实现见**第六节**。

---

本文档与当前代码一致；若后续代码有变更，请同步更新本文档中对应章节的描述与接口路径、字段名等，以保持“零基础可读”和“与技术实现一致”。
