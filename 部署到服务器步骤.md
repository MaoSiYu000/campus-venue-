# 更新上传到服务器 — 一步一步操作指南

按顺序执行下面步骤即可把本地更新部署到服务器。

---

## 第一步：在本地提交并推送到 Gitee（在你自己电脑上）

在项目根目录 `E:\2026study\software` 下打开终端（PowerShell 或 CMD），执行：

```bash
# 1. 进入项目目录
cd E:\2026study\software

# 2. 查看有哪些改动（可选）
git status

# 3. 把所有改动加入暂存区
git add .

# 4. 提交，引号里可以改成你的说明
git commit -m "项目更新：预约状态自动同步、个人主页布局、项目书等"

# 5. 推送到 Gitee（你之前用的远程名是 gitee）
git push gitee main
```

如果第 5 步提示要输入账号密码，按 Gitee 的账号密码或令牌输入即可。

**做完第一步后**，代码已经在 Gitee 上了，接下来去**服务器**上拉取并重启。

---

## 第二步：登录到服务器

在终端用 SSH 登录你的服务器（把下面的 IP 换成你自己的，如 120.26.124.180）：

```bash
ssh root@你的服务器IP
```

输入服务器密码后，就进入了服务器的命令行。

---

## 第三步：在服务器上进入项目目录并拉取最新代码

假设你在服务器上把项目克隆在 `/root/campus-venue` 或 `/var/www/campus-venue`，请按你**实际路径**改下面的命令：

```bash
# 进入项目目录（请改成你服务器上的实际路径）
cd /root/campus-venue
# 或者：cd /var/www/campus-venue

# 拉取 Gitee 上最新代码
git pull gitee main
```

如果服务器上还没有配置 gitee 远程，需要先添加：

```bash
git remote add gitee https://gitee.com/maosiyu26/campus-venue.git
git pull gitee main
```

---

## 第四步：在服务器上安装依赖并构建前端（如有改动）

```bash
# 仍在项目根目录下

# 前端：安装依赖（若从未装过或 package.json 有变化）
cd frontend
npm install
npm run build
cd ..
```

`npm run build` 会在 `frontend/dist` 下生成打包好的静态文件。若你用 Nginx 部署前端，需要让 Nginx 指向这个 `dist` 目录（或你配置的目录）。

---

## 第五步：在服务器上安装依赖并重启后端

```bash
# 在项目根目录

# 后端：安装依赖（若从未装过或 package.json 有变化）
cd backend
npm install
cd ..
```

然后**重启后端进程**，方式取决于你当前是怎么跑后端的：

**方式 A：用 PM2 跑（推荐）**

```bash
# 重启名为 backend 或 campus-api 的进程（名字按你实际取的来）
pm2 restart backend
# 或
pm2 restart all
```

**方式 B：用 node 直接跑**

先结束旧进程（在运行后端的终端里按 `Ctrl+C`），再重新启动：

```bash
cd E:\2026study\software\backend   # 在服务器上改成实际路径，例如：
cd /root/campus-venue/backend
npm run start:prod
# 或开发环境：npm run start:dev
```

若用 `start:prod`，需要先执行过 `npm run build`（在 backend 目录下）。

---

## 第六步：确认是否生效

1. **前端**：浏览器打开你的网站地址，强制刷新（Ctrl+F5），看页面是否已更新。
2. **后端**：登录、预约等接口能正常使用，说明后端已更新。

---

## 常见问题

- **git pull 报错“有本地修改”**：  
  若确定服务器上的修改可以丢弃，可先执行：  
  `git checkout .`  
  再 `git pull gitee main`。若不能丢弃，请先备份或提交后再拉取。

- **前端访问 404 或空白**：  
  检查 Nginx 的 `root` 是否指向 `frontend/dist`（或你实际打包输出目录）。

- **后端接口 502 / 连不上**：  
  检查后端进程是否在跑：`pm2 list` 或 `ps aux | grep node`；检查 `.env` 里数据库等配置是否正确。

---

把你**服务器上项目的实际路径**和**后端是用 PM2 还是直接 node 跑**发给我，我可以按你的环境把第三步到第五步的命令改成可直接复制执行的版本。
