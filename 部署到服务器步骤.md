# 更新上传到服务器 — 一步一步操作指南

按顺序执行下面步骤即可把本地更新部署到服务器。

---

## 第一步：在本地提交并推送到 Gitee（在你自己电脑上）

在项目根目录 `E:\2026study\software` 下打开终端（PowerShell 或 CMD），执行：

```bash
# 1. 进入项目目录
cd E:\2026study\software

# 2. 查看有哪些改动（可选）
git status

# 3. 把所有改动加入暂存区
git add .

# 4. 提交，引号里可以改成你的说明
git commit -m "项目更新：预约状态自动同步、个人主页布局、项目书等"

# 5. 推送到 Gitee（你之前用的远程名是 gitee）
git push gitee main
```

如果第 5 步提示要输入账号密码，按 Gitee 的账号密码或令牌输入即可。

**做完第一步后**，代码已经在 Gitee 上了，接下来去**服务器**上拉取并重启。

---

## 第二步：登录到服务器

在终端用 SSH 登录你的服务器（把下面的 IP 换成你自己的，如 120.26.124.180）：

```bash
ssh root@你的服务器IP
```

输入服务器密码后，就进入了服务器的命令行。

---

## 第三步：在服务器上进入项目目录并拉取最新代码

假设你在服务器上把项目克隆在 `/root/campus-venue` 或 `/var/www/campus-venue`，请按你**实际路径**改下面的命令：

```bash
# 进入项目目录（请改成你服务器上的实际路径）
cd /root/campus-venue
# 或者：cd /var/www/campus-venue

# 拉取 Gitee 上最新代码
git pull gitee main
```

如果服务器上还没有配置 gitee 远程，需要先添加：

```bash
git remote add gitee https://gitee.com/maosiyu26/campus-venue.git
git pull gitee main
```

---

## 第四步：在服务器上安装依赖并构建前端（如有改动）

```bash
# 仍在项目根目录下

# 前端：安装依赖（若从未装过或 package.json 有变化）
cd frontend
npm install
npm run build
cd ..
```

`npm run build` 会在 `frontend/dist` 下生成打包好的静态文件。若你用 Nginx 部署前端，需要让 Nginx 指向这个 `dist` 目录（或你配置的目录）。

---

## 第五步：在服务器上安装依赖并重启后端

```bash
# 在项目根目录

# 后端：安装依赖（若从未装过或 package.json 有变化）
cd backend
npm install
cd ..
```

然后**重启后端进程**，方式取决于你当前是怎么跑后端的：

**方式 A：用 PM2 跑（推荐）**

```bash
# 重启名为 backend 或 campus-api 的进程（名字按你实际取的来）
pm2 restart backend
# 或
pm2 restart all
```

**方式 B：用 node 直接跑**

先结束旧进程（在运行后端的终端里按 `Ctrl+C`），再重新启动：

```bash
cd E:\2026study\software\backend   # 在服务器上改成实际路径，例如：
cd /root/campus-venue/backend
npm run start:prod
# 或开发环境：npm run start:dev
```

若用 `start:prod`，需要先执行过 `npm run build`（在 backend 目录下）。

---

## 第六步：确认是否生效

1. **前端**：浏览器打开你的网站地址，强制刷新（Ctrl+F5），看页面是否已更新。
2. **后端**：登录、预约等接口能正常使用，说明后端已更新。

---

## 常见问题

- **git pull 报错“有本地修改”**：  
  若确定服务器上的修改可以丢弃，可先执行：  
  `git checkout .`  
  再 `git pull gitee main`。若不能丢弃，请先备份或提交后再拉取。

- **前端访问 404 或空白**：  
  检查 Nginx 的 `root` 是否指向 `frontend/dist`（或你实际打包输出目录）。

- **后端接口 502 / 连不上**：  
  检查后端进程是否在跑：`pm2 list` 或 `ps aux | grep node`；检查 `.env` 里数据库等配置是否正确。

---

## 服务器 502、头像上传失败（接口/静态资源都 502）

本地正常、到服务器就 502，多半是**后端没被正确访问**或**上传被 Nginx/权限拦住**。按下面逐项检查。

### 1. 确认 Nginx 把接口和上传请求转给后端

Nginx 需要把 `/api` 和 `/uploads` 转发到后端（例如本机 3000 端口）。在服务器上编辑站点配置（如 `/etc/nginx/sites-available/你的站` 或 `nginx.conf` 里的 `server`），保证有类似配置：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;
    root /root/campus-venue/frontend/dist;   # 改成你前端的 dist 实际路径
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 接口与上传必须转发到后端
    location /api/ {
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 10M;   # 上传文件大小限制，头像/策划书需要
    }
    location /uploads/ {
        proxy_pass http://127.0.0.1:3000/uploads/;
        proxy_set_header Host $host;
    }
}
```

- `proxy_pass` 的端口要和你后端监听的端口一致（默认 3000）。  
- **必须加 `client_max_body_size 10M;`**，否则大一点的头像或策划书会被 Nginx 直接拒绝，可能表现为 502 或 413。  
改完后执行：`nginx -t` 检查语法，再 `systemctl reload nginx`（或 `service nginx reload`）。

### 2. 确认后端在跑且没崩

在服务器上执行：

```bash
pm2 list
```

看 backend 是否 `online`。若是 `stopped` 或 `errored`，执行：

```bash
pm2 logs backend
```

看报错（如数据库连不上、端口占用、缺少 .env 等）。修好后执行 `pm2 restart backend`。  
若没用 PM2，用 `ps aux | grep node` 看是否有 node 进程在跑后端，并在运行后端的终端里看是否有报错。

### 3. 上传目录权限

后端会把上传文件写到「当前工作目录下的 uploads」（或你在 `.env` 里配的 `UPLOAD_DIR`）。  
若用 PM2，工作目录一般是启动时所在目录（例如 `backend`）。在服务器上执行：

```bash
cd /root/campus-venue/backend   # 改成你的 backend 实际路径
ls -la uploads
ls -la uploads/avatars
```

若没有 `uploads` 或 `uploads/avatars`，先建目录并给写权限：

```bash
mkdir -p uploads/avatars uploads/proposals uploads/venues
chmod -R 755 uploads
```

若 PM2 是用 root 起的，一般没问题；若是别的用户，要保证该用户对 `uploads` 有写权限。

### 4. 建议在服务器 .env 里固定上传目录（可选）

在 `backend/.env` 里加上（路径按你服务器实际来）：

```env
UPLOAD_DIR=/root/campus-venue/backend/uploads
```

这样无论从哪启动，上传都写到同一目录，方便排查。改完重启后端：`pm2 restart backend`。

### 5. 再测一遍

- 浏览器访问：`http://你的域名或IP`，登录后进个人资料上传头像。  
- 若仍 502：看 `pm2 logs backend` 是否有新报错；看 Nginx 错误日志：`tail -f /var/log/nginx/error.log`。  
- 若接口返回 413：说明请求体过大，确认 Nginx 里 `client_max_body_size 10M;` 已加并已 `reload nginx`。

---

把你**服务器上项目的实际路径**和**后端是用 PM2 还是直接 node 跑**发给我，我可以按你的环境把第三步到第五步的命令改成可直接复制执行的版本。
