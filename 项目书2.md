# 校园场地预约系统 — 项目报告

---

## 一、技术架构概览

- **前端**：Vue 3 + TypeScript + Element Plus + Pinia + Vue Router  
- **后端**：NestJS + TypeORM + MySQL  
- **认证**：JWT（JSON Web Token）+ 角色守卫（RolesGuard）  
- **密码**：bcrypt 加密存储，不明文保存  

---

## 二、设计模式的选取

- **单例（Singleton）**  
  - Nest 的 Service、Repository 在应用内默认单例，同一类只有一个实例，共享状态与连接（如 TypeORM 的 Repository 背后共用数据源）。

- **仓库模式（Repository）**  
  - TypeORM 的 `Repository<T>` 封装对单张表的增删改查，业务层通过 `repo.find()`、`repo.save()`、`repo.remove()` 等操作，不直接写 SQL，便于切换数据库与统一访问方式。

- **守卫 / 拦截器（Guard / Interceptor）**  
  - **Guard**：在请求进入 Controller 方法前执行。`JwtAuthGuard` 校验 JWT 并挂载用户到 `request.user`；`RolesGuard` 根据 `@Roles()` 校验角色，未通过则直接返回 403，与业务逻辑分离。  
  - **Interceptor**：`FileInterceptor('file')` 在进入上传处理方法前处理 multipart，将文件写入磁盘并注入 `@UploadedFile()`，Controller 只关心业务返回值。

- **观察者 / 响应式（Observer）**  
  - Vue 的 `ref`、`reactive`、`computed` 使数据变化自动驱动视图更新；Pinia 的 store 也是响应式，多组件共享同一状态（如 `user`、`role`、`announcementsConfirmed`），路由守卫与页面均依赖同一份状态。

- **装饰器（Decorator）**  
  - 使用 `@Get()`、`@Post()`、`@UseGuards()`、`@Roles()`、`@CurrentUser()`、`@Body()`、`@Query()` 等声明式描述路由、权限与参数来源，减少重复的校验与取值代码，逻辑集中在业务方法内。

---

## 三、登录界面：三种身份的设计与实现

### 3.1 设计思路

- **一个登录页**：身份下拉框 + 同一套表单完成三种身份登录；后端只提供 `POST /auth/login`，请求体带 `role`、`account`、`password`。  
- **标签随身份变化**：选“学生/老师”显示“学号”，选“场地管理员”显示“工号”，选“系统管理员”显示“用户名”，由前端 `computed` 根据 `form.role` 返回。

### 3.2 前端实现（Login.vue）

- 表单：`form.role`、`form.account`、`form.password`。提交调用 `login(...)` 即 POST /auth/login，成功后 `store.setLogin(payload)` 并持久化到 localStorage，根据 `payload.role` 跳转：user 若需强制改密跳 `/user/change-password` 否则 `/user/announcements`，venue_admin 跳 `/venue-admin/announcements`，system_admin 跳 `/system-admin/overview`。失败用 `ElMessage.error` 提示。

### 3.3 后端实现

- 控制器接收 `LoginDto`（role、account、password）。按 role 查对应表（user 表 studentId、venue_admin 表 workId、system_admin 表 username），用 bcrypt 校验密码，失败抛 UnauthorizedException。通过后用 JwtService.sign() 签发 JWT（payload 含 sub、role、id），返回 accessToken、role、id 及姓名电话头像等；学生/老师还有 mustChangePassword。

### 3.4 路由与权限

- `beforeEach`：目标需登录（meta.role）未登录则重定向 `/`；已登录但 meta.role 与 store.role 不一致则重定向该角色首页。学生/老师、场地管理员若 `!store.announcementsConfirmed`，除公告页与改密页外一律重定向公告页。

---

## 四、学生/老师界面功能说明

### 4.1 公告区域

- **整体流程**：进入公告页，未确认时先弹窗展示必读公告与站内通知，主界面为“公告”与“站内通知”两个区块。  
- **必读公告**：GET /announcements/must-read。后端查必读且目标含当前角色的公告，再查 user_announcement_read 得已读 id 集合，相减得未读列表；前端 onMounted 调 getMustRead() 赋给 modalMustRead，弹窗内卡片展示，每条有“确认”按钮。  
- **历史公告**：GET /announcements/must-read/history，后端取已读必读；前端 getMustReadHistory() 赋给 historyList，主界面公告区块只读展示。  
- **确认**：点击确认调 POST /announcements/mark-read，body { announcementIds: [id] }；后端插入 user_announcement_read；前端从 modalMustRead 移除并可插入 historyList。  
- **不看完不能进入**：store.announcementsConfirmed 来自 sessionStorage；登录时 user/venue_admin 会清空并设 false。弹窗内必读与通知全部确认后显示“全部确认并且进入”，点击后 setAnnouncementsConfirmed() 并跳 /user/venue-list。路由守卫在未确认时仅放行公告页与改密页。  
- **站内通知**：GET /notifications，后端按 target_type/target_id 查 notification 表；展示为卡片列表，未读左边框高亮（class unread）。学生端可 DELETE /notifications/:id 删除。产生时机：预约通过/驳回、场地不可用导致驳回时后端 NotificationService.create 插入通知。

### 4.2 个人主页：上传头像、编辑资料、修改密码、退出登录

- **上传头像**：编辑资料弹窗内 el-upload，accept="image/*"，:before-upload="onAvatarSelect"，在 before-upload 内调 uploadAvatar(file) 即 POST /upload/avatar（FormData 字段 file），return false 阻止自动上传。后端 FileInterceptor('file') + diskStorage 存 uploads/avatars/，文件名时间戳+随机数+扩展名，限制 2MB，返回 { path }。前端收到 path 写入 editForm.avatar 并更新预览；保存资料时 PATCH /auth/profile 带 avatar，store.setProfile 更新。  
- **编辑资料**：弹窗预填 store.user 的 name、phone、头像；保存调 updateProfile（PATCH /auth/profile），成功后 setProfile 并关闭。后端按 JWT role 和 id 更新对应用户表。  
- **修改密码**：跳 /user/change-password，表单原密码、新密码，POST /auth/change-password；后端 bcrypt 校验后写新哈希，学生若被标必须改密则清除该标记。  
- **退出登录**：store.logout() 清空 token/user 及 localStorage、sessionStorage 的 announcementsConfirmed，再 router.push('/')。

### 4.3 预约申请与文件上传

- **进入**：场地详情页“预约此场地”跳 /user/booking-apply?venueId=xxx，或菜单直接进再选场地。  
- **表单与时段校验**：场地、使用日期、开始/结束时间、活动名称、主办单位、预计人数、负责人、联系电话、说明、策划书（可选）。日期 el-date-picker 禁止选今天之前；时间 el-time-select 步长 30 分钟，若选今天则开始时间从当前之后第一个半点起。时段可用性：GET /booking-applications/check-availability，后端判断是否已过、同场地同天 pending/approved 时间重叠（start_time < 请求 end 且 end_time > 请求 start）、是否有不可用时段，返回 { available, conflict, hasUnavailableSlot, past }；前端据此展示并提交前再次校验。  
- **策划书上传**：el-upload accept=".pdf,.doc,.docx" :auto-upload="false"，onChange 中调 uploadProposal(file) 即 POST /upload/proposal，后端限制 10MB 存 uploads/proposals/，返回 path；前端赋给 form.proposalDocumentPath，提交预约时一并传后端。  
- **提交**：POST /booking-applications，后端再次校验后插入 status=pending；成功后跳“我的预约”。

### 4.4 场地列表、筛选与我的预约

- **场地列表**：GET /venues 可带查询参数，后端 QueryBuilder 按 venue_type、容量、location(LIKE)、has_projector、has_sound、is_available 等过滤；前端卡片网格展示，点击进详情。  
- **筛选**：页面上方筛选表单（类型、容量、位置、设备、可用），查询时组参调用 getVenueList(params)，重置清空再 load()。  
- **我的预约**：GET /booking-applications/my，表格展示；取消仅对待审核/已通过且未开始的开放，POST /booking-applications/:id/cancel，后端 status 改为 cancelled。

---

## 五、场地管理员界面功能说明

### 5.1 公告与“不看完不能进入”

- 与学生端相同：未确认时路由守卫停留在 /venue-admin/announcements；弹窗必读+站内通知，确认后“全部确认并且进入”跳 /venue-admin/applications。后端使用同一套 GET /announcements/must-read、must-read/history、POST mark-read，按 JWT role=venue_admin 筛选 targetRole 为 venue_admin 或 all 的必读公告，并在 user_announcement_read 按 userId+role 记录已读。

### 5.2 个人主页、修改密码、退出登录

- 与学生/老师一致：getProfile、updateProfile、uploadAvatar、PATCH /auth/profile、POST /auth/change-password、logout，后端按 JWT id 与 role=venue_admin 操作 venue_admin 表。路由为 /venue-admin/change-password，退出后 router.push('/')。

### 5.3 审核申请与预约总览

- **审核申请**（/venue-admin/applications，el-tabs：待审核、审批记录）：待审核数据 GET /booking-applications/pending，后端根据管理员 id 查管辖场地列表，再查 venue_id 在该列表且 status=pending 的记录，带 venue、user，按 createdAt 正序。表格列含 ID、场地、日期、时间段、活动名、申请人、操作（详情、通过、驳回）。通过 POST .../approve，驳回 POST .../reject body { rejectReason }，后端改状态并写 approved_by、approved_at，并 NotificationService.create 发站内通知。审批记录 GET /booking-applications/history，同场地范围但不限 status，按 createdAt 倒序，表格多列状态与审批时间，无操作按钮。  
- **预约总览**（/venue-admin/overview）：与系统管理员预约总览一致，两个页签“详细信息”“可视化分析”，GET /booking-applications/all、getVenueList，删除 DELETE /booking-applications/:id；后端 GET all 与 DELETE 允许 system_admin 与 venue_admin。

### 5.4 场地管理

- **列表**：GET /venues/managed/list 返回当前管理员管辖场地，表格列 ID、名称、类型、容量、位置、照片数、状态、操作（编辑、不可用时段）。  
- **编辑**：点击编辑打开弹窗，表单含名称、位置、地址、容量、开放时间、设备、可用、场地照片。上传照片：el-upload accept="image/*" :before-upload="beforePhotoUpload"，内调 uploadVenuePhoto(file) 即 POST /upload/venue-photo（FormData 字段 file），限制 15MB，返回 path  push 入 editForm.photos；保存 PUT /venues/:id 提交全部字段，后端校验权限（venue_admin_venue）后更新 venue 表；若 isAvailable 改为 0 会驳回该场地相关预约并发通知。  
- **不可用时段**：弹窗填 startTime、endTime、reason，POST /venues/:id/unavailable-slots；后端插入 venue_unavailable_slot 并对重叠的 pending/approved 预约驳回并发通知。  
- 学生端场地列表与详情中的 photos 来自同一 venue 表，列表用首图作封面，详情展示全部照片。

---

## 六、系统管理员界面功能说明

### 6.1 预约总览（全校）

- 页签“详细信息”“可视化分析”。onMounted 调 getAllBookings()、getVenueList()；GET /booking-applications/all 返回全校预约，filteredList 按 statusFilter（全部/未驳回/已驳回）过滤，表格含删除；DELETE /booking-applications/:id 物理删除。可视化：选场地后按 TIME_SLOTS 时段统计 successList（approved/used）中该场地与时段重叠数；另按场地汇总近 30 天预约次数，条形展示。均前端用已有数据计算，无新接口。

### 6.2 场地管理（全校）

- 手动添加 POST /venues；下载模板 GET /venues/import-template；Excel 导入 el-upload before-upload 发 POST /venues/import；恢复测试 POST /venues/ensure-test；一键清空（保留测试）POST /venues/clear body { keepTestOnly: true }；完全清空、删除选中同理。筛选 GET /venues 带 query。编辑与场地管理员一致：名称、位置、地址、容量、开放时间、设备、可用、场地照片（上传逻辑同 4.2.1/5.4 上传照片），PUT /venues/:id。

### 6.3 账号管理

- 页签“学生/老师”“场地管理员”。GET /system-admin/users、GET /system-admin/venue-admins（含 venues）。手动添加 POST .../accounts/users 或 .../venue-admins；下载模板、批量导入 Excel；恢复测试、一键清空、完全清空、删除选中；发送通知 POST notify-user / notify-venue-admin，后端 NotificationService.create；管辖范围 POST venue-admins/:id/scope body { venueIds } 更新多对多。

### 6.4 发布公告与历史公告

- 发布：表单标题、内容、必读开关、目标角色，POST /announcements；后端 announcement 表 insert，created_by 为当前管理员。历史：GET /announcements 列表，表格列标题、发布时间（日期上一行时间下一行）、必读、目标角色、操作（详情、删除）；详情弹窗展示全文；DELETE /announcements/:id 需 system_admin，先删 user_announcement_read 再删公告。

### 6.5 个人主页

- /system-admin/profile。编辑资料可改用户名、电话、头像，PATCH /auth/profile 含 username、phone、avatar，后端系统管理员分支校验用户名不重复。近期活动：GET /announcements/my 取当前管理员发布的公告，按 createdAt 倒序取前 7 条，每条标题加粗+内容前十字+发布时间。

---

## 七、数据结构和算法

### 7.1 数据结构

- **线性表（数组/顺序表）**  
  - **用途**：预约列表、公告列表、通知列表、场地列表等，在内存或数据库中按顺序存储；前端用 `ref<Array>()`、后端用 TypeORM 的 `find()` 返回数组。  
  - **增**：后端 `repo.save(entity)` 或 `repo.create()` 后 `save`；前端列表 `push` 或替换为新数组。  
  - **删**：后端 `repo.remove(entity)` 或 `repo.delete(id)`；前端 `list.filter(x => x.id !== id)` 或接口重拉。  
  - **改**：后端 `repo.findOne()` 后改属性再 `save`；前端找到对应项改字段或整表替换。  
  - **查**：后端 `repo.find({ where, order })`、`createQueryBuilder` 多条件；前端从接口取回后存到 ref 的数组，按需 `filter`、`slice`（如“最近 7 条”）。

- **集合（Set）**  
  - **用途**：需要 O(1) 判重、包含关系时使用。  
  - **后端**：公告服务中“已读公告 id 集合”`readIds = new Set(r.map(x => x.announcementId))`，再从必读列表中 `filter(a => !readIds.has(a.id))` 得未读、`filter(a => readIds.has(a.id))` 得已读历史；场地筛选时用 `bookedVenueIds = new Set(...)` 存某时段已被预约的场地 id，再 `list.filter(v => !bookedVenueIds.has(v.id))` 得可用场地。  
  - **前端**：公告页 `confirmedAnnouncementIds = ref<Set<number>>(new Set())`，单条确认后 `confirmedAnnouncementIds.value = new Set([...confirmedAnnouncementIds.value, a.id])`，用于判断是否显示“确认”按钮及“全部确认”是否可点。  
  - **增**：`set.add(id)`；**删**：`set.delete(id)` 或重建 Set；**查**：`set.has(id)`。

- **树（路由树）**  
  - **用途**：Vue Router 的 `routes` 为树形结构。根为 `/`（登录），一级子节点为 `/user`、`/venue-admin`、`/system-admin`，每个节点下 `children` 为子路由（如 `/user/profile`、`/user/venue-list`）；叶子节点对应一个 `component`。  
  - **查**：根据当前 URL 的 path 从根向下匹配，得到唯一匹配的叶子配置（含 `meta.role`、要渲染的组件）；路由守卫 `beforeEach` 中根据 `to.meta` 与 `store.role` 做权限判断。  
  - 不在此树上做“增删改”，配置在代码中写死。

- **键值映射（对象 / Map）**  
  - **用途**：前端 Pinia 的 `user`、`token`，表单对象 `editForm`；后端 JWT 的 payload（如 `id`、`role`、`sub`）、查询条件对象。  
  - **增/改**：`obj[key] = value` 或 `map.set(key, value)`；**删**：`delete obj[key]` 或 `map.delete(key)`；**查**：`obj[key]` 或 `map.get(key)`。

### 7.2 主要算法

- **区间重叠判断（时间段冲突）**  
  - **问题**：判断两个时间段是否重叠。约定区间为 `[start, end]`（左闭右闭或左闭右开均可，本项目中用时间点比较）。  
  - **条件**：区间 A 与 B 重叠当且仅当 **A.start < B.end 且 A.end > B.start**。  
  - **使用位置**：  
    - 预约冲突：`booking-application.service.ts` 中 `checkConflict` 用 SQL 条件 `(b.start_time < :endTime AND b.end_time > :startTime)` 查同场地、同天、状态为待审核/已通过的预约，若有记录则冲突。  
    - 不可用时段：`checkAvailability` 中查 `venue_unavailable_slot` 表，同样用“新时段与不可用区间是否重叠”判断；`getVenueDaySlots` 拉取某日某场地的预约与不可用时段用于时间轴展示。  
    - 场地列表“排除某时段已预约”：`venue.service.ts` 的 `findAll` 中，对已预约记录做 `filter(b => b.startTime <= excludeEnd && b.endTime >= excludeStart)` 得到在该时段占用的预约，再取这些预约的 `venueId` 构成 Set，从场地列表中过滤掉这些 id。  
  - **复杂度**：单次比较 O(1)；与数据库结合时由查询与索引决定。

- **排序**  
  - **场景**：列表按时间或主键有序展示。  
  - **实现**：后端 `order: { createdAt: 'DESC' }`、`orderBy('n.created_at', 'DESC')`、`order: { useDate: 'DESC', startTime: 'DESC' }` 等，使“最新在前”或“按日期+时间”排列；前端对已取回的数组如需二次排序可用 `sort`。  
  - **典型**：公告/通知按创建时间倒序；预约列表按使用日期、开始时间倒序或正序（时间轴正序）。

- **过滤与多条件查询**  
  - **过滤**：从全集里筛出满足条件的子集。例如“未读必读公告”= 必读公告列表 `filter(a => !readIds.has(a.id))`；“某时段可用场地”= 场地列表 `filter(v => !bookedVenueIds.has(v.id))`。  
  - **多条件**：场地列表按类型、容量、位置、设备、可用性等筛选用 TypeORM `QueryBuilder` 的 `andWhere` 动态拼接，避免 N+1 且统一在一句查询中完成。

---

## 八、如何使用本软件

### 8.1 数据库

```bash
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS campus_venue CHARACTER SET utf8mb4;"
mysql -u root -p campus_venue < database/V1__create_tables.sql
mysql -u root -p campus_venue < database/V2__seed_data.sql
```

### 8.2 后端

```bash
cd backend
npm install
npm run seed
npm run start:dev
```

若已使用旧版数据库，需执行一次迁移以支持个人资料中的学院/专业/班级与头像功能：

```bash
cd backend
mysql -u root -p campus_venue < scripts/add-user-profile-fields.sql
```

环境变量（可选，在 `backend` 目录下建 `.env`）：

```
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=你的密码
DB_DATABASE=campus_venue
JWT_SECRET=你的密钥
UPLOAD_DIR=uploads
```

后端默认运行在 **http://localhost:3000**，API 前缀为 `/api/v1`。

### 8.3 前端

```bash
cd frontend
npm install
npm run dev
```

前端默认运行在 **http://localhost:5173**，开发时已配置代理将 `/api` 和 `/uploads` 转发到后端。

### 8.4 测试账号（种子数据）

| 角色         | 账号     | 密码   | 说明 |
|--------------|----------|--------|------|
| 学生/老师    | 2024001  | 123456 | 首次登录需修改密码 |
| 场地管理员   | va001    | 123456 | 管辖前两个场地 |
| 系统管理员   | admin    | 123456 或 admin123 | 执行 `npm run seed` 后为 admin123 |

1. **启动**：后端 `npm run start:dev`，前端 `npm run dev`，浏览器访问前端地址。  
2. **登录**：选择身份，输入对应账号密码，点击登录。  
3. **学生/老师**：首次需在公告页确认完必读公告后点“全部确认并且进入”，然后可浏览场地、预约、我的预约、个人资料与修改密码。  
4. **场地管理员**：同样先确认公告，再使用审核申请、预约总览、场地管理、公告、个人主页。  
5. **系统管理员**：登录后直接使用预约总览、场地管理、账号管理、发布与历史公告、个人主页等。
