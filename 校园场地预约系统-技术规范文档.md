# 校园场地预约系统 — 技术规范文档

**文档版本**：v1.0  
**编写角色**：系统架构师  
**最后更新**：2026-02-15  

---

## 一、项目概述

### 1.1 系统定位

校园场地预约系统面向高校师生与管理人员，提供场地信息查询、在线预约申请、审批流转、状态维护及通知公告等能力，支持学生/老师、场地管理员、系统管理员三类角色。

### 1.2 角色与能力边界

| 角色 | 英文标识 | 主要能力 |
|------|----------|----------|
| 学生/老师 | `user` / `student_teacher` | 登录、查看场地、提交预约、管理我的预约、接收通知 |
| 场地管理员 | `venue_admin` | 登录、审核预约、维护场地状态、查看预约详情、接收/触发通知 |
| 系统管理员 | `system_admin` | 登录、全局场地与参数管理、账号与权限管理、发布公告 |

---

## 二、技术栈规范

### 2.1 总体原则

- **前后端分离**：前端与后端通过 RESTful API 通信，便于独立部署与扩展。
- **技术选型统一**：同一层次内（如前端、后端、数据库）只采用一套主技术栈，避免混用多种框架。
- **版本锁定**：依赖版本在依赖文件中明确指定，不依赖 `latest` 或宽范围版本。

### 2.2 推荐技术栈（规范方案）

| 层次 | 技术选型 | 版本建议 | 说明 |
|------|----------|----------|------|
| **前端** | Vue 3 + TypeScript | Vue 3.4+ / TS 5.x | 组合式 API + `<script setup>`，类型安全 |
| **前端构建** | Vite | 5.x | 构建与开发服务器 |
| **前端 UI** | Element Plus 或 Ant Design Vue | 与 Vue 3 匹配的最新稳定版 | 统一组件与交互规范 |
| **前端状态/路由** | Pinia + Vue Router | 与 Vue 3 匹配 | 全局状态与路由 |
| **前端 HTTP** | Axios | 1.x | 统一封装请求、鉴权、错误处理 |
| **后端** | Java + Spring Boot 或 Node.js + Nest.js | Spring Boot 3.x / Nest 18+ | 二选一，全项目统一 |
| **ORM/数据访问** | MyBatis-Plus（Java）或 TypeORM/Prisma（Node） | 与后端框架匹配 | 与所选后端一致 |
| **数据库** | MySQL | 8.0+ | 主业务库 |
| **缓存** | Redis | 7.x | 会话、验证码、简单缓存 |
| **文件存储** | 本地磁盘或 MinIO / 云存储 OSS | - | 策划书、场地照片等 |
| **认证** | JWT（Access + Refresh）或 Session + Redis | - | 与后端技术栈一致 |
| **API 风格** | RESTful | - | 资源命名、HTTP 方法、状态码统一 |

**前端与 HTML**：前端**使用 HTML**，体现在两处：（1）项目根入口 **`index.html`**，作为单页应用的挂载页，引入打包后的 JS/CSS；（2）各 **`.vue` 单文件组件** 中的 **`<template>`** 部分，书写的是类 HTML 的模板语法（可含 Vue 指令）。页面结构仍由 HTML 标签组成，Vue 负责数据绑定与交互，并非替代 HTML。

### 2.3 技术栈选型说明

- **前端**：Vue 3 + TS + Vite 为当前主流组合，便于维护与团队协作；Element Plus / Ant Design Vue 可快速实现管理端与部分 C 端界面。
- **后端**：  
  - **Java + Spring Boot**：适合校内已有 Java 技术栈、需要与现有系统集成的场景。  
  - **Node.js + Nest.js**：适合全栈偏前端团队、希望前后端统一语言的场景。  
- **数据库**：MySQL 满足事务、关联查询与报表需求；若需全文检索可后续增加 Elasticsearch。
- **缓存**：Redis 用于登录态、验证码、接口限流及部分热点数据缓存。

---

## 三、命名规范

### 3.1 通用原则

- **语义化**：名称能准确表达业务含义，避免无意义的 `a`、`data`、`info`。
- **一致性**：同一概念在全项目中使用同一英文术语（如场地 `venue`、预约 `booking`、申请 `application`）。
- **语言**：代码、表名、字段名、接口路径、变量名均使用**英文**；前端展示与文档可使用中文。

### 3.2 核心业务术语（中英对照）

| 中文 | 英文（代码/表/接口用） |
|------|------------------------|
| 场地 | venue |
| 预约 / 预约申请 | booking / booking_application |
| 用户（学生/老师） | user / student_teacher |
| 场地管理员 | venue_admin |
| 系统管理员 | system_admin |
| 公告 / 通知 | announcement / notification |
| 策划书 | proposal_document / proposal |
| 审核 | approval / review |
| 驳回 | reject / rejection |
| 管辖范围 | managed_scope / jurisdiction |

### 3.3 数据库命名

- **表名**：小写 + 下划线，复数或业务单数统一（建议单数），如 `venue`、`booking_application`、`venue_admin`、`announcement`。
- **字段名**：小写 + 下划线，如 `created_at`、`student_id`、`venue_id`、`status`。
- **主键**：表名单数 + `_id`，如 `id` 或 `venue_id`（若全局统一用 `id` 亦可）。
- **外键**：关联表名 + `_id`，如 `venue_id`、`user_id`。
- **布尔**：`is_` 前缀，如 `is_available`、`is_deleted`。
- **状态**：`status` 或 `xxx_status`，取值用枚举/常量，如 `pending`、`approved`、`rejected`、`cancelled`、`used`。

### 3.4 后端命名（以 Java/Nest 为例）

- **包/模块**：小写，多词用点或路径分隔，如 `com.xxx.venue`、`com.xxx.booking`。
- **类名**：大驼峰（PascalCase），如 `VenueService`、`BookingController`。
- **方法/函数**：小驼峰（camelCase），动词开头，如 `getVenueById`、`createBooking`、`rejectApplication`。
- **常量/枚举**：全大写下划线，如 `BOOKING_STATUS_PENDING`、`VenueType.REPORT_HALL`。
- **接口路径**：RESTful，小写 + 连字符，如 `/api/v1/venues`、`/api/v1/booking-applications`、`/api/v1/venue-admins`。

### 3.5 前端命名

- **目录**：小写，多词用连字符，如 `venue-list`、`booking-apply`、`my-bookings`。
- **组件文件**：PascalCase 或 kebab-case 与项目约定一致，如 `VenueCard.vue` 或 `venue-card.vue`。
- **组件名**：与文件名一致，PascalCase 在模板中使用，如 `<VenueCard />`。
- **变量/函数**：小驼峰，如 `venueList`、`submitBooking`。
- **常量**：全大写下划线或 const 对象，如 `BOOKING_STATUS`。
- **路由 path**：小写连字符，与目录对应，如 `/venue/list`、`/my-bookings`。
- **环境变量**：全大写下划线，如 `VITE_API_BASE_URL`。

### 3.6 接口（API）命名

- **基础路径**：`/api/v1`（版本可随迭代递增）。
- **资源**：名词复数或业务单数统一，如 `/venues`、`/booking-applications`、`/announcements`。
- **HTTP 方法**：GET 查询、POST 创建、PUT/PATCH 更新、DELETE 删除。
- **子资源**：`/venues/:id`、`/booking-applications/:id/approve`、`/booking-applications/:id/reject`。
- **查询与过滤**：Query 参数小写下划线，如 `?venue_type=report_hall&min_capacity=50`。
- **请求/响应体**：JSON，键名小驼峰（若后端为 Java 可配置 JSON 序列化为 camelCase）。

---

## 四、目录与模块结构建议

### 4.1 前端（Vue 3 项目示例）

```
frontend/
├── public/
├── src/
│   ├── api/              # 接口封装，按模块划分
│   │   ├── venue.ts
│   │   ├── booking.ts
│   │   ├── announcement.ts
│   │   └── auth.ts
│   ├── assets/
│   ├── components/       # 公共组件
│   │   ├── common/
│   │   └── layout/
│   ├── layouts/          # 布局（学生/管理员等）
│   ├── router/
│   ├── stores/           # Pinia
│   ├── types/            # TS 类型
│   ├── utils/            # 工具与常量
│   ├── views/            # 页面，按角色或功能分子目录
│   │   ├── user/         # 学生/老师
│   │   ├── venue-admin/
│   │   └── system-admin/
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

### 4.2 后端（Spring Boot 示例）

```
backend/
├── src/main/java/com/xxx/campusvenue/
│   ├── config/           # 安全、跨域、Redis 等
│   ├── controller/       # 按资源划分
│   │   ├── VenueController.java
│   │   ├── BookingApplicationController.java
│   │   ├── AnnouncementController.java
│   │   └── AuthController.java
│   ├── service/
│   ├── mapper/            # MyBatis-Plus
│   ├── entity/
│   ├── dto/               # 请求/响应 DTO
│   ├── enums/             # 状态等枚举
│   ├── exception/         # 统一异常与处理
│   └── CampusVenueApplication.java
├── src/main/resources/
│   ├── application.yml
│   └── mapper/
└── pom.xml
```

（若采用 Nest.js，可对应为 `modules/venue`、`modules/booking`、`modules/announcement`、`modules/auth` 等。）

### 4.3 数据库脚本与文档

- 建表、索引、初始数据脚本放在 `database/` 或 `docs/sql/`，按版本或日期命名，如 `V1__create_tables.sql`。
- 核心表结构说明、ER 关系可写在本文档或单独 `docs/数据库设计.md`。

---

## 五、安全与认证规范

### 5.1 认证

- **学生/老师**：学号 + 密码；首次登录强制修改密码（身份证后六位仅作初始密码，不存储明文）。
- **场地管理员**：工号 + 密码。
- **系统管理员**：独立超级管理员账号。
- 密码存储：仅存储加盐哈希（如 BCrypt），禁止明文或可逆加密存储。

### 5.2 鉴权与权限

- 所有需登录接口校验 Token（JWT 或 Session）。
- 按角色与数据范围鉴权：  
  - 学生/老师：仅可操作本人预约与可见场地。  
  - 场地管理员：仅可操作管辖范围内场地的申请与状态。  
  - 系统管理员：可操作全局配置、账号与公告。
- 敏感操作（如驳回、取消、修改场地不可用）记录操作人、时间与原因，便于审计。

### 5.3 接口与数据安全

- 生产环境使用 HTTPS。
- 登录、修改密码等接口防暴力破解：限流 + 验证码（可选）。
- 文件上传：限制类型与大小，策划书、照片等存储路径不直接暴露内部结构；下载时校验权限。

---

## 六、通知与公告

- **公告**：系统级，由系统管理员发布；支持“登录后必读”，确认后进入主界面。
- **通知**：可针对个人或角色，如预约结果、场地不可用提醒。实现方式可选：站内消息表 + 前端轮询/WebSocket、邮件或企业微信（若学校有统一出口）。
- 场地或时间段被设为“不可用”时，系统自动向已预约该时段用户发送通知（内容需包含场地、原时间段、原因等）。

---

## 七、状态与枚举约定

### 7.1 预约申请状态（示例）

| 状态值（英文） | 含义 |
|----------------|------|
| pending | 待审核 |
| approved | 已通过 |
| rejected | 已驳回 |
| used | 已使用 |
| cancelled | 已取消 |

### 7.2 场地可用性

- 场地级：`is_available`（整体是否开放）。
- 时间段级：独立“不可用时段”表或字段，支持按日期+时段标记维护/占用，与预约校验结合，避免重复预约或冲突。

---

## 八、文档与版本

- 本技术规范作为开发与协作的基准，新增模块或表、接口需符合命名与技术栈约定。
- 接口变更、表结构变更应在接口文档（如 Swagger/OpenAPI）或数据库设计文档中更新，并注明版本与日期。
- 建议在项目根目录维护 `README.md`（项目介绍、本地运行说明）、`CHANGELOG.md`（版本变更摘要）。

---

## 九、附录：角色与功能映射表（便于实现对照）

| 功能点 | 学生/老师 | 场地管理员 | 系统管理员 |
|--------|-----------|------------|------------|
| 登录（学号/工号/超管） | ✓ | ✓ | ✓ |
| 首次登录强制改密 | ✓（学生/老师） | 可选 | 否 |
| 登录后公告确认 | ✓ | ✓ | 可选 |
| 查看/筛选场地列表与详情 | ✓ | ✓（管辖） | ✓（全部） |
| 提交预约申请 | ✓ | - | - |
| 我的预约（状态、取消） | ✓ | - | - |
| 待审核列表、通过/驳回 | - | ✓ | - |
| 维护场地信息与不可用 | - | ✓（管辖） | ✓（全部） |
| 预约总览/详情 | - | ✓（管辖） | ✓（全部） |
| 账号管理、管辖范围、单独通知 | - | - | ✓ |
| 发布全局公告 | - | - | ✓ |
| 退出登录 | ✓ | ✓ | ✓ |

---

*本文档仅规定技术栈、命名与架构规范，不包含具体代码实现。开发时请严格遵循本规范，以保证可维护性与团队协作一致性。*
